import { Meta, StoryObj } from '@storybook/react';
import { HabitsList } from "../components/HabitsList";

export default {
  title: "Components/HabitsList",
  component: HabitsList,
} as Meta;

export const Default: StoryObj = {};
